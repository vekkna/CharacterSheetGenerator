function createCharacter(){
    //Find the input field containing the name (use its id)
    //store its value in a variable
    //put that name into the p in the character sheet (find the p by its id)
}